package com.sivalabs.bookmarker.domain.utils

/*

object SeleniumUtils {

    @Throws(Exception::class)
    fun captureScreenMethod() {
        System.setProperty("webdriver.chrome.driver", "/Users/sivaprasad.reddy/mybin/chromedriver")
        val options = ChromeOptions()
        options.addArguments("--headless", "--disable-gpu", "--window-size=1920,1200", "--ignore-certificate-errors")
        val driver: WebDriver = ChromeDriver(options)
        driver.get("https://sivalabs.in")
        // driver.manage().window().maximize()
        val screenshotFile: File = (driver as TakesScreenshot).getScreenshotAs(OutputType.FILE)
        FileUtils.copyFile(screenshotFile, File("/Users/sivaprasad.reddy/data/a.png"))
        driver.close()
        driver.quit()
    }
}
*/
